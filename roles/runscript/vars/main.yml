---
scripttorun: sample.sh
targethost: all
fetchdir: /tmp
arg1: "{{ '' | default('') }}"
arg2: "{{ '' | default('') }}"
tgtuser: sudo1 #If you want to run as root use assudo / asroot
logsdir: /tmp/runscript